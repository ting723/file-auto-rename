# 开发思路

- 从指定目录中相关文件-> 判断是否指定的格式 -> 计算该文件的md5值 -> 重新命名该文件

## 文件重命名方式

- 只支持视频文件修改的名称
- 采用文件的扩展名的方式判断是否是视频格式: mp4、avi、mkv、mov、wmv、flv、webm
- 使用md5的方式修改文件名称
- 仅支持目录文件的方式修改

## 打包成终端命令的方式

- rust 默认打包方式，打包成可执行命令

## 自动集成到 Mac 的可执行环境中

- 使用install.sh 命令安装
